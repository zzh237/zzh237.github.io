\documentclass{article}

\usepackage[latin1]{inputenc}
\usepackage{tikz}
\usetikzlibrary{trees}
\usepackage{verbatim}

\begin{comment}
:Title: Probability tree
:Tags: Trees

A simple example of a probability tree diagram. 

\end{comment}


\begin{document}
\pagestyle{empty}


% Set the overall layout of the tree
\tikzstyle{level 1}=[level distance=1.5cm, sibling distance=6cm]
\tikzstyle{level 2}=[level distance=1.5cm, sibling distance=4cm]
\tikzstyle{level 3}=[level distance=1.5cm, sibling distance=2cm]

% Define styles for bags and leafs
\tikzstyle{bag} = [text width=4em, text centered]
\tikzstyle{end} = [circle, minimum width=3pt,fill, inner sep=0pt]



% The sloped option gives rotated edge labels. Personally
% I find sloped labels a bit difficult to read. Remove the sloped options
% to get horizontal labels. 
\begin{tikzpicture}[grow=down]
\node[end] {}
    child {
        node[end] {}        
            child {
                node[end, label=below:{$C: 00$}] {}
                edge from parent
                node[above] {0}
                node[below]  {}
            }
            child {
                node[end, label=below:
                    {}] {}
		child {
			node[end, label=below:{$E: 010$}] {}
			edge from parent
			node[above] {0}
			node[below]  {}
			}
		child {
			node[end, label=below:{$D: 011$}] {}
			edge from parent
			node[above] {1}
			node[below]  {}
			}
                edge from parent
                node[above] {1}
                node[below]  {}
            }
            edge from parent 
            node[above] {0}
            node[below]  {}
    }
    child {
        node[end] {}        
        child {
                node[end, label=below:
                    {$B: 10$}] {}
                edge from parent
                node[above] {0}
                node[below]  {}
            }
            child {
                node[end, label=below:
                    {$A: 11$}] {}
                edge from parent
                node[above] {1}
                node[below]  {}
            }
        edge from parent         
            node[above] {1}
            node[below]  {}
    };
\end{tikzpicture}

\end{document}